import React from "react";

const ErrorImageMessage = ({ classes }) => (
  <div className="row justify-content-center">
    <div className="message-image">
      <div className={`col ${classes} background-image`} />
    </div>
  </div>
);

export default ErrorImageMessage;
